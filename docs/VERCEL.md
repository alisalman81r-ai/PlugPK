# Deploying plug.pk to Vercel

The application is ready. Everything that blocked a deploy has been changed in
the repository; what is left is provisioning and four commands, and none of it
requires touching code.

This file is the runbook. Follow it in order — step 2 has to happen before the
first deploy or the build fails at page-data collection.

---

## What changed to make this deployable

| Was | Now |
| --- | --- |
| SQLite file at `prisma/dev.db`, gitignored | Postgres, `provider = "postgresql"` |
| 19 SQLite migrations using `PRAGMA` / `DATETIME` | One Postgres migration, 25 tables, generated by `prisma migrate diff` |
| `contains` relying on SQLite's case-insensitive LIKE | `mode: 'insensitive'` on all five predicates |
| Uploads written to `public/uploads` | Vercel Blob when `BLOB_READ_WRITE_TOKEN` is set, filesystem when it is not |
| No database export | `data/db-export/`, all 155 rows, round-trip verified |
| `next@14.2.0`, 2 critical advisories | `next@14.2.35`, 0 critical |
| No `prisma generate` on install | `postinstall: prisma generate` |

The data is unchanged. `scripts/export-db.ts` wrote every row of all 25 tables
to `data/db-export/`, and re-importing that export into an empty database and
exporting it again produced byte-identical files. Nothing was added, dropped or
rewritten — 48 cars, 6 stations, 14 connectors, 23 reviews, 12 services, 12
community posts, 23 comments, 8 clubs, 2 crawl sources, 5 source records, 2
crawl runs.

---

## 1. Create a Postgres database

Neon, Supabase or Vercel Postgres. Free tiers are fine for this data volume.
Copy the connection string; it looks like:

```
postgresql://user:password@host/database?sslmode=require
```

## 2. Create the schema and load the data

Run these locally, against the new database, before the first deploy. The
build prerenders 137 pages by querying the database, so an empty or absent one
fails the build.

```bash
export DATABASE_URL="postgresql://..."     # PowerShell: $env:DATABASE_URL="..."

npx prisma migrate deploy                  # creates all 25 tables
npx tsx scripts/import-db.ts               # loads data/db-export/
```

`import-db.ts` refuses to run against a database that already has rows, so it
cannot double up if it is run twice. It works out insert order by retrying
rows whose parents are not in yet, so no manual ordering is needed.

Expect `155 rows imported in 1 pass.`

## 3. Point local development at the same database

Put the same URL in `.env`. There is no SQLite fallback any more, and that is
deliberate: two engines means `contains` is case-insensitive on one and not the
other, which is a bug that only appears in production. Use a second Neon branch
if you want local writes kept away from live data.

`prisma/dev.db` is still on disk and still gitignored. Keep it until the
Postgres database has been checked over — it is the only copy of anything the
export missed.

## 4. Connect the project to Vercel

Import `alisalman81r-ai/PlugPK`, production branch `main`. No `vercel.json` is
needed: App Router detection, build command and output directory are all correct
by default, and `postinstall: prisma generate` is already in `package.json`.

Set these under **Settings → Environment Variables**:

| Variable | Notes |
| --- | --- |
| `DATABASE_URL` | From step 1. Set it for Production, Preview and Development. |
| `SESSION_SECRET` | Signs user and admin cookies. **Generate a new one** — do not reuse the local value. |
| `ADMIN_PASSWORD` | Admin portal login. |
| `ENABLE_ADMIN` | Leave **unset** unless `/admin` is meant to be reachable in production. Without it those routes 404. |
| `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` | Compiled into the client bundle and readable by anyone — restrict it by HTTP referrer to the deployed domain. |
| `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID` | Map styling. |
| `CRAWLER_ENABLED` | Leave off unless the crawler should run in production. |
| `CRAWLER_TRIGGER_SECRET` | Required only if the crawler endpoint is exposed. |

Anything named `NEXT_PUBLIC_` is public. That is correct for the Maps key and
wrong for everything else, so do not rename a secret into that prefix to reach
it from a component.

## 5. Attach a Blob store

**Storage → Create → Blob**, attached to the project. Vercel sets
`BLOB_READ_WRITE_TOKEN` automatically, and admin image uploads start writing
there instead of to the filesystem.

Skipping this does not break the deploy. The public site does not depend on it —
the 48 catalogue photographs are committed under `public/images/cars/` — but
admin uploads will throw until the store exists, because there is no writable
disk to fall back to.

## 6. Deploy

Push to `main`. Vercel builds automatically.

---

## Checking it worked

- `/cars` lists 48 cars.
- **Search a lowercase brand.** `byd` must return the BYD cars. This is the one
  behaviour that changed with the engine and the one most likely to be wrong if
  something was missed.
- `/cars/compare?ids=byd-seal,kia-ev5` fills every row of the table.
- `/map` shows 6 stations.
- Upload an image in the admin portal, if the Blob store is attached.

## Known and deliberate

**Next has 8 high advisories left and no critical ones.** Clearing the rest
requires Next 16, which is a major upgrade — React 19 and App Router changes —
and is not something to do in the same change as a database migration.
`14.2.35` is the most patched release on the 14.2 line and closes both criticals,
including the middleware authorization bypass, which matters here because
`/admin` is gated in middleware.

The other advisories are `eslint-config-next`, `prisma` CLI and their
dependencies. Those are devDependencies and are not deployed.

**The crawler's single-writer notes are now pessimistic.** Several comments
(`crawler/live-deps.ts`, `src/lib/db/car-source-store.ts`,
`src/app/api/crawler/daily/route.ts`) explain batching and concurrency limits in
terms of a single-writer SQLite file. Postgres does not have that constraint, so
those limits are now conservative rather than necessary. Nothing is broken; the
reasoning behind the numbers has just stopped applying.
